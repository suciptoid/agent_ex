Removed the unused `ask_agent` and `handover` tool modules along with their dead test stubs, normalized the remaining legacy delegation metadata label, and updated the release server wrapper to run migrations before starting the app.

By: gpt-5.4-mini on Github Copilot
